[pytest]
markers =
    openfoam: marks tests that require a running OpenFOAM installation (deselect with '-m "not openfoam"')
    platform_sensitive: marks tests that may produce non-deterministic results across platforms due to trimesh OBB computation differences

pythonpath = simulation_worker
